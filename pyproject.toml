[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aml-job-hunter"
version = "0.1.0"
description = "AI-powered AML KYC job hunter"
requires-python = ">=3.13"
dependencies = [
    "workers-py>=1.90",
    "workers-runtime-sdk"
]

[tool.setuptools]
py-modules = []